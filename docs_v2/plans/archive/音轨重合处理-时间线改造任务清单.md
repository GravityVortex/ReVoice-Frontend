# 音轨重合处理 & 时间线改造任务清单（按实施顺序）

日期：2026-02-06  
依据方案：`docs_v2/plans/archive/音轨重合处理-时间线改造实施方案.md`

说明：
- 这是“可落地”的工程任务清单（可直接按 checkbox 推进）。
- 所有改造遵循：KISS、默认不惊扰用户、失败不阻塞编辑、后端协议向后兼容。

---

## M0 Legacy 合成链路（不改外部服务，先保证“改时间线 = 改输出”）

- [x] 批量保存 timing：更新 subtitleData 的 `start/end/id`（对 legacy id 做重命名）
  - 落点：`src/app/api/video-task/update-subtitle-timings/route.ts`
- [x] R2 覆盖复制：`adj_audio_time/<oldId>.wav` → `adj_audio_time/<newId>.wav`（通过现有 Java overwrite-file API）
  - 落点：`src/app/api/video-task/update-subtitle-timings/route.ts`
- [x] 返回 `idMap(old->new)` 并在前端同步本地状态（避免下一次保存找不到旧 id）
  - 落点：`src/app/[locale]/(dashboard)/video_convert/video-editor/[id]/page.tsx`
- [x] 合成接口保持 legacy：只传 `audio_clips: string[]`（避免外部服务兼容风险）
  - 落点：`src/app/api/video-task/generate-video/route.ts` + `src/shared/services/pythonService.ts`

---

## M1 前端时间线：同轨不重叠 + Shift Ripple（不引 wavesurfer）

### 1) 纯函数碰撞/吸附策略（先写对）
- [x] 实现 collision solver（move + ripple）
  - 新增：`src/shared/lib/timeline/collision.ts`
- [x] 单元测试覆盖边界（重合/贴边/越界/连锁 ripple）
  - 新增：`src/shared/lib/timeline/collision.test.ts`

### 2) UI 交互落地（拖拽只在落点提交，避免卡）
- [x] 字幕片段支持拖动（pointer events），拖动中只做视觉预览，`pointerup` 才提交 state
  - 改造：`src/shared/components/video-editor/subtitle-track.tsx`
- [x] 冲突反馈：回弹 + 200ms 红描边 + 1px 红线（极简、可感知）
  - 改造：`src/shared/components/video-editor/subtitle-track.tsx`（必要时补少量 CSS）
- [x] Shift Ripple：仅在冲突发生且按住 Shift 时推开后续；否则吸附卡住
  - 改造：`src/shared/components/video-editor/subtitle-track.tsx`
- [x] dirtyMap：记录被改动的字幕 timing（id -> startMs/endMs）
  - 改造：`src/app/[locale]/(dashboard)/video_convert/video-editor/[id]/page.tsx`
- [x] UI 顶部显示极简状态：`已修改 · 待保存 N`（已存在 pendingChanges，可复用）
  - 改造：`src/app/[locale]/(dashboard)/video_convert/video-editor/[id]/page.tsx`

---

## M2 批量保存字幕 timing（避免 N 次 update）

- [x] 新增批量 API：`POST /api/video-task/update-subtitle-timings`
  - 新增：`src/app/api/video-task/update-subtitle-timings/route.ts`
- [x] DB 更新为“单次写回整段 JSON”（不要循环 update）
  - 改造：`src/app/api/video-task/update-subtitle-timings/route.ts`
- [x] “合成视频”前自动保存 dirtyMap（成功后再触发合成）
  - 改造：`src/app/[locale]/(dashboard)/video_convert/video-editor/[id]/page.tsx`
- [x] 验收：长字幕（300+）拖动多次后，点击合成只产生 1 次保存请求 + 1 次合成请求

---

## M3 wavesurfer.js（第一阶段：长波形背景参考，不重写播放引擎）

- [x] 引入依赖并做 client-only dynamic import（避免 SSR）
  - 改造：`package.json` + `src/shared/components/video-editor/waveform-backdrop.tsx`
- [x] 私桶签名 URL 的 CORS 兜底：增加同源代理 `/api/storage/proxy?src=...`，避免 wavesurfer WebAudio 解码时 `Failed to fetch`
  - 新增：`src/app/api/storage/proxy/route.ts`
  - 改造：`src/app/[locale]/(dashboard)/video_convert/video-editor/[id]/timeline-panel.tsx`
- [x] 波形只加载“可见轨道”，并发限制 3~4，支持 AbortController 取消
  - 新增：`src/shared/lib/waveform/loader.ts`
- [x] UI：波形占位 skeleton + 顶部轻提示（不阻塞编辑）
  - 改造：`src/app/[locale]/(dashboard)/video_convert/video-editor/[id]/timeline-panel.tsx`
- [x] 验收：首屏可编辑；波形后台补齐（波形失败不影响编辑）

---

## M4 限制补齐：音频超窗提示（外部服务不改）

- [x] 记录并展示每段音频 `durationMs`（可视区域懒加载），并在 UI 标记“超窗”
- [x] 引导操作：延长当前段（若有空隙）/ Shift Ripple 推开后续（提示文案）

---

## 工程性验收（必须满足）

- [x] 同轨不重叠不变量：任意时刻 `end(i) <= start(i+1)`（保存前/后都成立）
- [x] 性能：拖动不触发全列表高频 re-render（拖动过程仍流畅）
- [x] 稳定：波形加载失败不影响编辑；请求取消后不产生 setState on unmounted
- [x] 兼容：未升级 Python 时，legacy 合成链路仍正常
